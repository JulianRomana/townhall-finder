<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}
            <h1>Hello world</h1>
            {{ form_start(search_form) }}
                {% include 'form.html.twig' %}
            {{ form_end(search_form) }}

            {% if townHallInfos is defined  %}
             <ul>
                 <li> nom de la mairie : {{ townHallInfos['name'] }}</li>
                 <li> adresse de la mairie : {{ townHallInfos['address'] }}</li>
                 <li> email de la mairie : {{ townHallInfos['email'] }}</li>
                 <li> site web de la mairie : {{ townHallInfos['website'] }}</li>
                 <li> Telephone de la mairie : {{ townHallInfos['phone'] }}</li>
             </ul>
            {% endif %}

            {% if error is defined %}
                <p>{{ error }}</p>
            {% endif %}
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
